import React from "react";

const Header = () => (
  <header className="fixed top-0 left-0 w-full z-50 bg-green-900 shadow h-16 flex items-center justify-center">
    <h1 className="text-2xl font-bold tracking-wide text-black">
      ZeroToOne
    </h1>
  </header>
);

export default Header;
